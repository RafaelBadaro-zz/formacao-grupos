
from modelos import Cromossomo


# Roleta viciada
def roletar(cromossomos, numero_populacao):

    # Pega 2 cromossomos, cruza e add em um vetor de filhos[], fazer isso ate ter o mesmo número de populacao, len(numero_populacao)

    # numero_nova_geracao = 0
    # nova_geracao = []
    # while(numero_populacao > numero_nova_geracao)
    # pai1_sorteado = sortear_pai(cromossomos)
    # cromossomos.remove(pai1_sorteado) # - resolve a regra do: Nao pode cruzar o mesmo cromossomo
    # pai2_sorteado = sortear_pai(cromossomos)
    # filho_nova_geracao = cruzarpais(pai1_sorteado, pai2_sorteado)
    # nova_geracao.add(filho_nova_geracao)
    # cromossomos.add(pai1_sorteado)


def sortear(cromossomos):
    # Roleta vem aqui
